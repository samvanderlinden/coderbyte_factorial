function FirstFactorial(num) { 

  // code goes here
  if(num === 0 || num ===1)
    return 1;

    for(var i = num - 1; i >= 1; i--) {
        num *= i;
    }
  
  return num; 
         
}

   
// keep this function call here 
FirstFactorial(readline());
